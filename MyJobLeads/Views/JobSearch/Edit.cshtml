@model MyJobLeads.DomainModel.Entities.JobSearch

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
    bool isNewJobSearch = Model.Id == 0;
}

@if (isNewJobSearch)
{
    <h2>Start a New Job Search</h2>
    <p>
        Your first step is to setup a job search. Interview Tools allows you to organize multiple job searches. Some uses of this are:
        <ul>
            <li>Organize your job search by year, such as "2011 Full Time Position", "2012 Internship Hunt", or "May 2012 Need Part-Time Job"</li>
            <li>Organize job search by function, such as "Sales and Marketing Opportunities" or "Engineering Job Search"</li>
        </ul>
    </p>
}
else
{
    <h2>Editing the '@Model.Name' job search</h2>
}

@using (Html.BeginForm(MVC.JobSearch.Edit()))
{
    @Html.HiddenFor(x => x.Id);
    
    <table>
        <tr>
            <td align="right">Job Search Name:</td>
            <td>@Html.TextBoxFor(x => x.Name)</td>
        </tr>

        <tr>
            <td align="right" valign="top">Description:</td>
            <td>@Html.TextAreaFor(x => x.Description, new { rows = 2, cols = "50" })</td>
        </tr>

        <tr>
            <td></td>
            <td>
                <input type="submit" value="Save" /> &nbsp; &nbsp;
                
                @if (isNewJobSearch) { @Html.ActionLink("Back", MVC.JobSearch.Index()); }
                else { @Html.ActionLink("Back", MVC.JobSearch.Details(Model.Id)); }
            </td>
        </tr>
    </table>
}

